// Data shape for the Mission and Vision section
export interface MissionAndVisionData {
    vision: {
      title: string
      description: string
    }
    mission: {
      title: string
      description: string
    }
  }